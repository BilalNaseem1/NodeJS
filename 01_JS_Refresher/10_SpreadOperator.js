// The spread operator (...) is used to expand an iterable such as a string or an array or object into its individual elements
// It is used to pass multiple arguments to a function

const numbers = [1, 2, 3, 4, 5];
const newNumbers = [...numbers, 6, 7, 8, 9, 10];
console.log(newNumbers);